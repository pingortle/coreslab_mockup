<ul class="nav nav-tabs nav-justified">
 {{#each model}}
	{{#if this.isToplevel}}
		{{#link-bootstrap}}
			{{#link-to 'workarea' this}}{{this.name}}{{/link-to}}
		{{/link-bootstrap}}
	{{/if}}
  {{/each}}
</ul>

{{outlet}}
